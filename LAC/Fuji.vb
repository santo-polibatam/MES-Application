Imports System.Data.SqlClient
Module Fuji

    'Public Sub read_setting_printer()
    '    Dim sql As String
    '    Dim ds As New DataSet





    '    Dim items() As String = (From item As String In Main.listprinter.Items() Select item).ToArray

    '    Dim index1 = Array.IndexOf(items, selectedPrinterFujiSIdeLabel)
    '    Dim index2 = Array.IndexOf(items, selectedPrinterFujifrontLongLabel)
    '    Dim index3 = Array.IndexOf(items, selectedPrinterFujiRotaryLabel)
    '    Dim index4 = Array.IndexOf(items, selectedPrinterFujiCartonLabel)
    '    Dim index5 = Array.IndexOf(items, selectedPrinterFujiOutSIdeLabel)
    '    Dim index6 = Array.IndexOf(items, selectedPrinterFujifrontShortLabel)

    '    If index1 >= 0 Then
    '        Main.cbx_fuji_side_label.SelectedText = selectedPrinterFujiSIdeLabel
    '        Main.label_side_printer.PrintSettings.PrinterName = selectedPrinterProduct

    '    Else
    '        listprinter.SelectedText = "Microsoft Print to PDF"
    '        Main.label_printer.PrintSettings.PrinterName = "Microsoft Print to PDF"
    '        MsgBox("Product label Printer are choosen (" & selectedPrinterProduct & ") Not installed in this PC!" & vbNewLine & "Printer will use: Microsoft Print to PDF")
    '    End If

    'End Sub

End Module
